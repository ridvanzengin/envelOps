# EnvelOps frontend

React + TypeScript + Vite. See [`../CLAUDE.md`](../CLAUDE.md) and
[`../docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md) §10 for the screen list
and §7 for the i18n approach (`react-i18next`, English + Turkish).

- `npm run dev` — dev server
- `npm run build` — type-check + production build
- `npm run lint` — oxlint
